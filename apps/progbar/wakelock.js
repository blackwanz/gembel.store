/* ============================================================
   PLACEHOLDER — wakelock.js was referenced by progbar.html
   (<script src="wakelock.js">) but was NOT included in your
   upload, so its real content is not available here.

   Paste your existing wakelock.js content into this file.
   It's the Screen Wake Lock API helper that keeps the display
   from sleeping while a Neon Flow session is active — same idea
   as the wake-lock logic already built into apps/meditate/meditate.js.

   Until you paste the real file in, progbar.html will still load
   and run fine — it just won't have wake-lock behavior (this file
   is intentionally a no-op below so nothing throws).
   ============================================================ */
