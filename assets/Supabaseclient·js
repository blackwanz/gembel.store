/* ============================================================
   SUPABASE CONNECTION + KONFIGURASI PEMBAYARAN
   ============================================================
   Koneksi ke Supabase, plus konstanta yang dipakai openPaymentModal()
   di assets/auth.js buat nulis baris baru ke tabel payment_request.
   ============================================================ */

(function () {
  const SUPABASE_URL = "https://cejmhfyyiftjsgtyzmvt.supabase.co";
  const SUPABASE_ANON_KEY = "sb_publishable_-VtjJUw4-6bpmTH4bKXdgQ_dVaPTMQ5";

  const client = window.supabase.createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
    auth: { persistSession: true, autoRefreshToken: true, detectSessionInUrl: true },
  });
  window.supabase = client;
})();

/* ---- PEMBAYARAN ----
   Alur sekarang: user klik upgrade -> insert baris ke payment_request
   (status 'pending') -> admin konfirmasi di admin.html -> plan user
   keupdate ke GEMBEL_PLAN_TARGET.

   Nanti kalau Xendit (atau payment gateway lain) udah siap, isi link
   invoice/payment di GEMBEL_PAYMENT_URL. Kosong = tombol bayar cuma
   bikin payment_request berstatus "menunggu konfirmasi admin". */
window.GEMBEL_PAYMENT_URL = "";
window.GEMBEL_PRICE = 19999;              // harga sekarang (IDR / bulan)
window.GEMBEL_PRICE_NORMAL = 1000000;     // harga "normal" buat banner diskon
window.GEMBEL_PLAN_TARGET = "elite";      // nilai yang ditulis ke payment_request.flag_premium
                                           // dan ke user_profiles.plan pas admin konfirmasi